{% extends 'base.html.twig' %}

{% block title %}privilege{% endblock %}

{% block body %}
    <h1>privilege</h1>
    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Nom Du Privilege</th>
            <th>Statut</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td>{{ privillege.id }}</td>
            <td>{{ privillege.privillegeName }}</td>
            <td><span class="label label-sm label-success">{{privillege.statut}}</span></td>
            <td>
                <a href="{{ path('app_privillege') }}" class="btn btn-tbl-arrow btn-xs">
                    <i class="fa fa-arrow-left"></i>
                </a>
                <a href="{#{{ path('', {'id': privillege.id}) }}#}" class="btn btn-tbl-edit btn-xs">
                    <i class="fa fa-pencil"></i>
                </a>
            </td>
        </tr>
        </tbody>
    </table>


    <a href="{{path('supprimer.privillege',{'id':privillege.id})}}">
        <button class="btn btn-tbl-delete btn-xs">
            <i class="fa fa-trash-o "></i>
        </button></a>
{%endblock%}
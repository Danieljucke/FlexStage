{% extends 'baseRestau.html.twig' %}

{% block contenu %}
    <h1>Liste des Plats</h1>
    {% for plats in plat %}
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h5 class="card-title">{{plats.nom_plat }}</h5>
            <p class="card-text">{{plats.prix }}</p>
            <p class="card-text">Id: {{ plats.id }}</p>
            <a href="#" class="card-link">Todo</a>

        </div>
    </div>
    {% endfor %}
{% endblock %}

